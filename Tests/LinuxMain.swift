import XCTest
@testable import CGPointVectorTests

XCTMain([
     testCase(CGAffineTransormTests.allTests),
     testCase(CGPointTests.allTests),
     testCase(CGRectTests.allTests),
     testCase(CGSizeTests.allTests),
])
